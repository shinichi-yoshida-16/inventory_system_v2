# 作業メモ（インフラ準備の実施手順）

未了だった2件を実際に片づけるための手順。正式版は [docs/manuals/deployment.md](docs/manuals/deployment.md)、
ここは「いま手を動かして検証を通す」ための作業メモ。

- プロジェクト: `inventory-system-507404`
- 検証コード: `verification/`（git 管理外。`src/.env` の SA 認証情報を読む）
- 検証で使っている SA: `inventory-system-dev@inventory-system-507404.iam.gserviceaccount.com`（現状スコープは Sheets のみ）

---

## 1. GCS バケット作成 ＋ SA へ権限付与（検証1・検証2 を通す）

### 1-1. gcloud の準備

```bash
gcloud auth login              # ブラウザ認証（プロジェクトのオーナー権限を持つアカウント）
gcloud config set project inventory-system-507404
gcloud services enable storage.googleapis.com
```

### 1-2. バケット作成（asia-northeast1）

```bash
export BUCKET=inventory-system-507404-state

gcloud storage buckets create gs://$BUCKET \
  --location=asia-northeast1 \
  --uniform-bucket-level-access \
  --public-access-prevention
```

### 1-3. SA に objectAdmin を付与

```bash
# いま検証コードが使っている SA（すぐ検証を回すため）
gcloud storage buckets add-iam-policy-binding gs://$BUCKET \
  --member="serviceAccount:inventory-system-dev@inventory-system-507404.iam.gserviceaccount.com" \
  --role="roles/storage.objectAdmin"
```

> 本番の Cloud Run 実行 SA（`inventory-run@…`、deployment.md 3.2 で新規作成）にも、後で同じ付与が必要。

### 1-4. `src/.env` にバケット名を追加

```
GCS_BUCKET=inventory-system-507404-state
```

### 1-5. 検証スクリプトを再実行

```bash
node verification/01_gcs_bucket.mjs
```

期待する結果:
- `pending/xxxx.json` の作成・読み出し・削除が成功する
- `locks/inventory.lock` を `ifGenerationMatch=0` で作成 → 2 回目は 412 で失敗する
- 取得した generation で削除できる／古い generation では削除が 412 になる

ここまで通れば FR-14 / FR-15 / FR-09 のマージ退避が実装可能な状態。

---

## 2. Gmail の SMTP + アプリパスワードで実送信（検証3 を通す）

> OAuth2（旧手順）はテスト公開ステータスだとリフレッシュトークンが約7日で失効し、恒久運用には
> Google の審査が必要だったため不採用。既存の Gmail アカウントに2段階認証＋アプリパスワードを
> 設定するだけの SMTP 方式に切り替えた。

### 2-1. 送信元アカウントの準備（既存アカウントを流用）

1. 送信元にする既存の Gmail アカウントで 2 段階認証を有効化（未設定なら myaccount.google.com/security）。
2. myaccount.google.com/apppasswords でアプリパスワードを発行
   - アプリ: 「メール」、デバイス: 「その他（名前を入力）」→ `inventory-system` などと入力
   - 表示された16桁のパスワードを控える（スペースは無視してよい）

### 2-2. `src/.env` に保存

```
GMAIL_SENDER=送信元アカウント@example.com
GMAIL_APP_PASSWORD=xxxxxxxxxxxxxxxx
```

本番格納は deployment.md 3.5 に相当する箇所を `gcloud secrets create` で Secret Manager に置き換える（`gmail-app-password` など）。

### 2-3. 実送信テスト

`verification/gmail_smtp_test.mjs`（nodemailer を使用。`verification/` に `npm install nodemailer` 済み）:

```bash
export TEST_TO=あなたのアドレス@example.com
node verification/gmail_smtp_test.mjs
```

受信できれば FR-09 の送信経路が確定。
